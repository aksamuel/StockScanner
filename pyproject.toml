[project]
name = "stockscanner"
version = "2.8.0"
description = "StockScanner — NYSE stock scanner with interactive HTML dashboard, position sizing, and GitHub Pages deployment."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "StockScanner User" }
]
dependencies = [
  "pandas",
  "numpy",
  "yfinance",
  "ta",
  "openpyxl"
]
keywords = ["stock", "scanner", "finance", "yfinance", "nyse", "dashboard", "position-sizing"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "License :: OSI Approved :: MIT License",
  "Operating System :: Microsoft :: Windows",
]

[project.scripts]
stockscanner = "stockscanner.cli:main"
download-nyse = "download_nyse:main"

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = [
  "download_nyse",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["stockscanner"]
