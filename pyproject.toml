[project]
name = "stockscanner"
version = "2.11.0"
description = "StockScanner — NYSE stock scanner with interactive HTML dashboard, position sizing, GitHub Pages deployment, and ChatGPT AI integration."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "StockScanner User" }
]
dependencies = [
  "pandas",
  "numpy",
  "yfinance",
  "ta",
  "openpyxl",
  "openai>=1.0.0",
  "python-dotenv>=0.19.0"
]
keywords = ["stock", "scanner", "finance", "yfinance", "nyse", "dashboard", "position-sizing", "chatgpt", "ai"]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "License :: OSI Approved :: MIT License",
  "Operating System :: Microsoft :: Windows",
]

[project.scripts]
stockscanner = "stockscanner.cli:main"
download-nyse = "download_nyse:main"

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = [
  "download_nyse",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["stockscanner"]
